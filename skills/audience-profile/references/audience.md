# Audience — profile

> Editable template. Replace every `<…>` placeholder. Keep personas to three at most; more than that and none of them are real.

## Primary persona — `<label, e.g. "Founder Maya">`

- **Role:** `<e.g. founder/CEO, 5–40 person company>`
- **Industry:** `<…>`
- **Buys / decides:** `<sole decision maker | recommends to a partner | needs board sign-off>`
- **Budget reality:** `<range, and what they compare it against>`
- **Where they read:** `<LinkedIn feed on mobile, ~90 seconds | newsletter at desk | Google search>`
- **Sophistication:** `<knows the category, can't evaluate craft>`

**Top pains (ranked)**
1. `<e.g. site looks like everyone else's and doesn't convert>`
2. `<e.g. previous agency shipped late and went quiet>`
3. `<e.g. can't tell good work from expensive work>`

**Goals**
- `<e.g. look credible enough to close bigger deals>`
- `<e.g. stop losing leads on mobile>`

**Objections, and what actually answers them**

| Objection | Answer that works |
|---|---|
| `<"too expensive">` | `<cost of the current leak, in their numbers>` |
| `<"how do I know you'll finish?">` | `<named process + fixed milestone dates>` |
| `<"can't AI do this now?">` | `<show the specific failure mode of generated work>` |

**Reading level:** `<plain English, grade 8–9. No jargon without a half-sentence gloss.>`
**Attention:** `<first 2 lines decide everything; hard cap ~200 words on social>`
**Do not:** `<no long intros, no theory, no design vocabulary, no "let's dive in">`

---

## Secondary persona — `<label>`

- **Role:** `<e.g. marketing manager who briefs the work>`
- **Cares about:** `<…>`
- **Fears:** `<being blamed for a bad vendor pick>`
- **Top pains:** `<1–3>`
- **Objections:** `<…>`
- **Reading level / attention:** `<…>`
- **Do not:** `<…>`

---

## Tertiary / edge persona — `<label>`

- **Who:** `<e.g. peer designers and devs — audience for credibility, not sales>`
- **Value to us:** `<referrals, hiring, reputation>`
- **What they want:** `<process detail, real craft, the parts others hide>`
- **Do not:** `<don't sell to them; don't dumb it down>`

---

## Not the audience

Say who to stop writing for, so drafts don't drift:

- `<e.g. enterprise procurement — different cycle, different copy>`
- `<e.g. total beginners looking for free templates>`

## Channel preferences

| Channel | Format that works | Length | Notes |
|---|---|---|---|
| LinkedIn | `<story + one lesson>` | `<120–200 words>` | `<no links in body; hook in line 1>` |
| X / Twitter | `<single sharp claim or short thread>` | `<1–5 posts>` | `<no threads under 3 real points>` |
| Newsletter | `<one idea, worked through>` | `<400–700 words>` | `<subject line is the hook>` |
| Blog / SEO | `<problem → approach → proof>` | `<900–1500 words>` | `<answer the query in the first 100 words>` |
| Landing page | `<outcome headline, proof, one CTA>` | `<scannable>` | `<one CTA repeated, not five>` |
| Carousel | `<one point per slide>` | `<6–10 slides>` | `<slide 1 = hook, last = ask>` |
| Sales email | `<observation → relevance → small ask>` | `<under 120 words>` | `<no attachments, one link>` |

## Proof the audience actually responds to

Ranked, most persuasive first:

1. `<before/after numbers from a comparable client>`
2. `<named client + specific outcome>`
3. `<visible craft — screenshots, live links>`
4. `<process transparency — what you do in week 1>`
5. `<testimonials — weakest; use as support, never as the argument>`
