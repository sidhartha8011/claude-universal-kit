# Business — context

> Editable template. Replace every `<…>` placeholder. Anything still in angle brackets is **not** a usable fact — treat it as `[NEEDS: …]` when drafting.
>
> **Last verified:** `<YYYY-MM-DD>`

## Company

- **Trading name:** `<…>`
- **Legal name:** `<…>`
- **Founded:** `<year>`
- **Based in:** `<city, country>` — **serves:** `<markets>`
- **Team size:** `<number, or "small senior team" if you'd rather not state it>`
- **Canonical contact:** `<hello@example.com>` / `<phone>`
- **Site:** `<https://…>`
- **Socials:** `<LinkedIn, X, Instagram>`

## What it does — in one line

`<e.g. We design and build websites and software for companies that need to look more credible than their competitors.>`

## Services

| Service | What it actually includes | Typical timeline | Price shape | Public? |
|---|---|---|---|---|
| `<Web design & development>` | `<scope in plain words>` | `<4–8 weeks>` | `<from ₹X / project>` | `<yes/no>` |
| `<Brand strategy>` | `<…>` | `<…>` | `<…>` | `<…>` |
| `<Digital marketing>` | `<…>` | `<…>` | `<…>` | `<…>` |
| `<Custom software / apps>` | `<…>` | `<…>` | `<…>` | `<…>` |
| `<UI/UX design>` | `<…>` | `<…>` | `<…>` | `<…>` |

**Not offered** (say no to these): `<e.g. print production, paid media buying under ₹X/mo, staff augmentation>`

## Positioning

- **Category:** `<the box the buyer already has in their head>`
- **For:** `<audience>` **who** `<situation>`
- **We are the:** `<one-line position>`
- **Unlike:** `<the default alternative>`
- **Because:** `<the reason it's credible>`

**Say it this way:** `<the exact sentence, verbatim, for reuse>`

## Differentiators

Only real, checkable ones. Each needs evidence.

| Differentiator | Evidence |
|---|---|
| `<senior people do the actual work — no juniors on delivery>` | `<how a prospect can verify>` |
| `<fixed milestone dates, published up front>` | `<…>` |
| `<design and engineering in one team, no handoff>` | `<…>` |

## Competitors / alternatives

| Alternative | Who picks it | Where they win | Where we win |
|---|---|---|---|
| `<big agency>` | `<…>` | `<scale, brand safety>` | `<speed, price, access to seniors>` |
| `<freelancer marketplace>` | `<…>` | `<cost>` | `<accountability, finishing>` |
| `<AI site builder>` | `<…>` | `<instant, near-free>` | `<doesn't look generic; actually converts>` |
| `<doing nothing>` | `<…>` | `<no cost, no risk>` | `<name the ongoing cost of the status quo>` |

Rule: describe alternatives fairly. Never name a competitor to insult them.

## Proof points

Only entries that are true and shareable. Mark anything under NDA.

| Client | What we did | Outcome (number) | Shareable? |
|---|---|---|---|
| `<name>` | `<…>` | `<e.g. +38% form completions in 60 days>` | `<yes / NDA / logo only>` |
| `<name>` | `<…>` | `<…>` | `<…>` |

**Awards / credentials:** `<…>`
**Volume claims allowed publicly:** `<e.g. "40+ projects since 2021" — only if true>`

## Goals

- **This quarter:** `<e.g. 4 new retainer clients in <sector>>`
- **This year:** `<…>`
- **What we're deliberately not chasing:** `<…>`
- **Primary conversion action:** `<e.g. book a 20-min call>` — everything public should point here.

## Constraints & sensitivities

- **Never say:** `<claims legal/clients won't allow, e.g. guaranteed rankings, specific client revenue>`
- **Under NDA:** `<clients or projects that cannot be named>`
- **Compliance:** `<any regulated claims to avoid>`
- **Tone limits:** `<e.g. no fake scarcity, no discount urgency>`

## Boilerplate

**Short bio (one line):**
`<…>`

**Standard 50-word about paragraph:**
`<…>`

**Standard CTA:**
`<…>`
