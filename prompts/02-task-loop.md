# Returning-Session Task Prompt

For any session AFTER onboarding is done. Short on purpose.

---

```
Read .claude/CODEBASE_MAP.md, CLAUDE.md, and the last 10 entries of
.claude/SESSION_LOG.md. Then work on the task below following the
Phase 4 task loop (restate → plan → implement → security gate →
verify → self-review → report + log).

If the map contradicts what you find on disk, update the map first.

TASK: <describe the task here>
```
